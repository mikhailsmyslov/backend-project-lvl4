.d-flex.flex-column.align-items-center
  h6.mb-4 Available statuses
  p.small.ml-2 Select status from a list
  select.selectpicker.form-control-lg.mb-5.select-link(title='Click to check statuses')
    each status in statuses
      option(
        value=status.id,
        selected=status.id === selectedStatusId
        data-href=urlFor("editStatus", status.id)
      )= status.name
  a(href=urlFor("newStatus"))
    i.fas.fa-plus.fa-lg.mr-2
    | New Status